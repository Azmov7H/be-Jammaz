# Jammaz Backend — Production-Like Local Environment
# This file is used by docker-compose.production-local.yml
# NEVER commit real secrets.

# MongoDB (containerized with auth)
MONGODB_URI=mongodb://jammaz_user:d46d8d405932268f9cfb0bdad4d5664b3851220956dd9b09@mongodb:27017/jammaz_production_local?authSource=admin

# JWT signing secret (MUST match frontend's JWT_SECRET)
JWT_SECRET=2LHRqHccAiz+5az0URcaYLA4HkDvnlajVqGnmXn+cdJceSaKw6MdJaA8iTUZ76Jx

# Access token lifetime (production-like: 15 minutes)
ACCESS_TOKEN_TTL=15m

# Refresh token lifetime in days
REFRESH_TOKEN_TTL_DAYS=30

# Server port (internal Docker port)
PORT=5000

# Production mode — enables security headers, strict CORS
NODE_ENV=production

# Cookie secure flag — must be false for local HTTP development
# (secure cookies require HTTPS; set to true when behind Nginx HTTPS on 8443)
COOKIE_SECURE=false

# CORS / OAuth
NEXT_PUBLIC_BASE_URL=http://localhost:8443
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:8443
ALLOW_ORIGIN_LESS=false

# Google OAuth (optional — leave empty if not using)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
OAUTH_AUTO_PROVISION=false

# Performance
DASHBOARD_CACHE_TTL=30

# Logging
LOG_TTL_DAYS=90
